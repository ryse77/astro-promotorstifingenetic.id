---
import { SITE_CONFIG } from '@/lib/constants';

interface Props {
  location?: string; // Optional: untuk personalisasi per lokasi
}

const { location } = Astro.props;
---

<section class="py-16 md:py-24 bg-gray-50">
  <div class="container mx-auto px-4">
    <div class="text-center mb-16">
      <h2 class="text-3xl md:text-4xl font-heading font-bold text-gray-900 mb-4">
        Keuntungan Menjadi Promotor STIFIn
        {location && <span class="text-primary-600"> di {location}</span>}
      </h2>
      <p class="text-xl text-gray-600 max-w-3xl mx-auto">
        Dapatkan penghasilan unlimited dengan sistem bisnis yang terbukti
      </p>
    </div>

    <div class="grid md:grid-cols-2 lg:grid-cols-3 gap-8">
      {SITE_CONFIG.business.benefits.map((benefit, index) => (
        <div class="bg-white p-6 rounded-xl shadow-lg hover:shadow-xl transition-all hover:-translate-y-1">
          <div class="flex items-start space-x-4">
            <div class="flex-shrink-0">
              <div class="w-12 h-12 bg-primary-100 text-primary-600 rounded-lg flex items-center justify-center font-bold text-xl">
                {index + 1}
              </div>
            </div>
            <div class="flex-1">
              <p class="text-gray-700 leading-relaxed">{benefit}</p>
            </div>
          </div>
        </div>
      ))}
    </div>

    <!-- Additional Info -->
    <div class="mt-12 bg-primary-50 border-l-4 border-primary-600 p-6 rounded-lg max-w-3xl mx-auto">
      <h3 class="font-bold text-primary-900 mb-2">Kenapa STIFIn?</h3>
      <p class="text-gray-700">
        STIFIn (Science of Talents through Fingerprint) adalah metode tes genetic berbasis sidik jari yang telah terbukti membantu ribuan orang menemukan potensi diri mereka. Sebagai promotor, Anda akan membantu menyebarkan manfaat ini sekaligus mendapatkan penghasilan yang menarik.
      </p>
    </div>
  </div>
</section>
