---
import { SITE_CONFIG } from '@/lib/constants';

interface Props {
  location?: string;
}

const { location } = Astro.props;
---

<section class="py-16 md:py-20 bg-white">
  <div class="container mx-auto px-4">
    <div class="max-w-6xl mx-auto">
      <!-- Header -->
      <div class="text-center mb-12">
        <div class="inline-block bg-primary-100 text-primary-700 px-6 py-2 rounded-full text-sm font-semibold mb-4">
          6 Sumber Penghasilan
        </div>
        <h2 class="text-3xl md:text-4xl font-heading font-bold text-gray-900 mb-4">
          Satu Profesi, 6 Revenue Streams
        </h2>
        <p class="text-lg text-gray-600 max-w-3xl mx-auto">
          Diversifikasi income Anda dengan 6 sumber penghasilan berbeda. Aktif atau pasif, semuanya bisa menghasilkan!
        </p>
      </div>

      <!-- Income Streams Grid -->
      <div class="space-y-6 mb-12">
        {SITE_CONFIG.business.incomeStreams.map((stream, index) => (
          <div class="bg-gradient-to-r from-primary-50 to-secondary-50 rounded-2xl p-6 md:p-8 hover:shadow-lg transition-all">
            <div class="flex flex-col md:flex-row md:items-center md:space-x-6">
              <!-- Number Badge -->
              <div class="flex-shrink-0 mb-4 md:mb-0">
                <div class="w-16 h-16 bg-gradient-to-br from-green-600 via-teal-600 to-cyan-600 text-white rounded-xl flex items-center justify-center font-bold text-2xl shadow-lg">
                  {index + 1}
                </div>
              </div>

              <!-- Content -->
              <div class="flex-1">
                <h3 class="font-bold text-xl text-gray-900 mb-2">{stream.title}</h3>
                <p class="text-gray-700 mb-3">{stream.description}</p>
                <div class="flex flex-wrap items-center gap-4">
                  <div class="bg-white px-4 py-2 rounded-lg">
                    <span class="text-sm text-gray-600">Potensi:</span>
                    <span class="font-bold text-primary-700 ml-2">{stream.potentialIncome}</span>
                  </div>
                  <div class="text-sm text-gray-600">
                    💡 {stream.example}
                  </div>
                </div>
              </div>
            </div>
          </div>
        ))}
      </div>

      <!-- Total Potential -->
      <div class="bg-gradient-to-br from-green-600 via-teal-600 to-cyan-600 rounded-2xl p-8 md:p-10 text-white text-center shadow-2xl">
        <div class="max-w-3xl mx-auto">
          <div class="inline-block bg-white/20 backdrop-blur-sm px-6 py-2 rounded-full text-sm font-semibold mb-4">
            Total Potensi Penghasilan
          </div>
          <h3 class="text-4xl md:text-5xl font-heading font-bold mb-4">
            Rp 10-20 Juta Per Bulan
          </h3>
          <p class="text-xl text-white/90 mb-6">
            Bahkan bisa mencapai <strong>>3x UMR DKI Jakarta</strong> tergantung aktivitas dan strategi Anda
          </p>
          <div class="grid md:grid-cols-3 gap-4 mt-8">
            <div class="bg-white/10 backdrop-blur-sm rounded-xl p-4">
              <div class="text-3xl font-bold mb-1">300%</div>
              <div class="text-sm text-white/80">Margin Minimal per Tes</div>
            </div>
            <div class="bg-white/10 backdrop-blur-sm rounded-xl p-4">
              <div class="text-3xl font-bold mb-1">6</div>
              <div class="text-sm text-white/80">Revenue Streams</div>
            </div>
            <div class="bg-white/10 backdrop-blur-sm rounded-xl p-4">
              <div class="text-3xl font-bold mb-1">∞</div>
              <div class="text-sm text-white/80">Unlimited Potential</div>
            </div>
          </div>
        </div>
      </div>

      <!-- Note -->
      <div class="mt-8 text-center">
        <p class="text-sm text-gray-600">
          * Potensi penghasilan bervariasi tergantung pada aktivitas, jumlah klien, dan strategi marketing masing-masing promotor
        </p>
      </div>
    </div>
  </div>
</section>
